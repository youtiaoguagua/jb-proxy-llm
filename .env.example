# 代理服务器配置模板文件
# 复制此文件为 .env 或 .env.local 并填入真实配置

# === 基础配置 ===
# 服务器监听端口
PROXY_PORT=8080

# === API服务器配置 ===
# 目标OpenAI兼容的API服务器地址
TARGET_HOST=https://api.example.com

# 授权令牌 - 请替换为您的实际API密钥
AUTH_BEARER=your-api-key-here

# === 日志配置 ===
# 日志级别: debug, info, error
LOG_LEVEL=info

# 调试模式: true/false, 1/0
# 调试模式会记录详细的请求和响应信息
DEBUG_MODE=false

# === 授权验证配置 ===
# 是否启用入参Authorization验证: true/false, 1/0
# 启用后，API端点需要有效的Bearer token才能访问
AUTH_VALIDATION=false

# 允许的token列表，逗号分隔
# 格式: token1,token2,token3 (不带Bearer前缀)
ALLOWED_TOKENS=

# 默认使用的Authorization头名称
DEFAULT_AUTH_HEADER=Authorization

# 管理员token（用于/admin/cache/clear等管理接口）
# 如果不设置，管理接口不需要认证
ADMIN_TOKEN=
